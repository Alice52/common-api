package common.token;

/**
 * todo: this module will add token(jwt) to header, which get from identity server. <br>
 * <br>
 * WIP:
 *
 * <pre>
 *      1. impl base-identity-server: issue token and validate token
 *      2. add token validate logic
 *      3. test common-token module by restTemplate and OpenFeign
 *      4. extra storage as alone layer, which can be impl by different strategy, such as memory, redis
 *  </pre>
 */
